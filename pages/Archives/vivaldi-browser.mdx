# vivaldi browser

- In Settings -> Keyboard -> Window -> Print, remove the shortcut
- In Settings -> Keyboard -> Window -> Quick Commands, set it to `Ctrl + P`

Now you can use `Ctrl + P` to change to the opened tabs.