# gallery 2024

import { isUrl } from "../../scripts/utils";
import { PHOTOS_2024 } from "data";

<div className="flex-col gap-2 flex">
  {PHOTOS_2024.map((photo, index) => {
    return (
      <GalleryImage
        key={photo.name}
        caption={photo.caption ?? index + 1}
        src={isUrl(photo.name) ? photo.name : `images/gallery/${photo.name}`}
      />
    );
  })}
</div>

```jsx
// <div className="hidden md:flex-col md:gap-2 md:flex">
// work in progress
<MobileGallery
  className="block md:hidden"
  photos={PHOTOS_2024.map((i) => i.name)}
/>
```
