# Install VSCode on Fedora

- https://code.visualstudio.com/docs/setup/linux#_rhel-fedora-and-centos-based-distributions

```bash
# Install the key and yum repository by running the following script:

sudo rpm --import https://packages.microsoft.com/keys/microsoft.asc
echo -e "[code]\nname=Visual Studio Code\nbaseurl=https://packages.microsoft.com/yumrepos/vscode\nenabled=1\nautorefresh=1\ntype=rpm-md\ngpgcheck=1\ngpgkey=https://packages.microsoft.com/keys/microsoft.asc" | sudo tee /etc/yum.repos.d/vscode.repo > /dev/null

# Then update the package cache and install the package using dnf (Fedora 22 and above):

dnf check-update
sudo dnf install code # or code-insiders
```

## extensions

- vim
- github copilot
  - sign in to use
- cline
- gitignore
- git graph v3
- gitlens
- gruvbox theme
- partial diff
- live server
- markdown all in one
- markdownlint
- simple react snippets
- Extension Pack for Java
- Tailwind CSS IntelliSense
- mdx


## settings

- remove keyboard shortcut ctrl + p in vim mode to enable default shortcut
- turn on auto save on focus change
- set editor word wrap: wordWrapColumn
- editor smooth scrolling
- add font 'Mononoki Nerd Font Mono'
- uncheck Editor: Experimental Edit Context Enabled to make Japanese input [work properly](https://github.com/VSCodeVim/Vim/issues/9672#issuecomment-2975557747)

```json
    "files.autoSave": "onFocusChange",
    "editor.formatOnSave": true,
    "editor.fontSize" = 21,
    "editor.fontLigatures" = true,
    "vim.foldfix": true,
    "vim.useSystemClipboard": true,
    "vim.easymotion": true,
    "vim.leader": ",",
    "git.enableSmartCommit": true,
    "git.confirmSync": false,
    "git.autofetch": true,
    "github.copilot.enable": {
        "*": true,
        "plaintext": false,
        "markdown": true,
        "scminput": false
    },
    "markdownlint.config": {
        "MD012": false, // Multiple consecutive blank lines
        "MD030": false, // Spaces after list markers
        "MD007": false  // Unordered list indentation
    },
```
