# vibe coding

I tried use github copilot agent in vscode insider, to create a bookmark application with MCP(Model Context Protocol). Failed. AI(Claude 3.7/3.5) just keep hallucinating, it can not even use the `@modelcontextprotocol/sdk` correctly.
