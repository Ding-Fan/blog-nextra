# sql exercise

テーブル名がtest、フィールドが数字のid、20文字のnameのテーブルを作成する
テーブル名がlog、フィールドが日付のLD、時間のLT、100文字のlogのテーブルを作成する
testテーブルに日付のudでフィールドを追加する、場所はnameの後ろとする
testテーブルのidのデータ型を8文字に変更する
usrテーブルのunameのフィールド名をusernameに変更する、データ型は15文字とする
usrテーブルのfamilyのフィールドを削除する
crashテーブルを削除する
scheduleテーブルからpdate・uid・memoを表示する
usrテーブルから、familyが2のuid・unameを表示する
usrテーブルにuidが「agatha」、unameが「christie」となるデータを追加する
テーブル名がmusic、フィールドが８文字のnum、２０文字のname、時間のmtのテーブルを作成する　numを主キーとする
テーブル名がadr、フィールドが数字のzip、100文字のadr、20文字のname、15文字のtelのテーブルを作成する　zipとnameにNOT NULLを指定する
musicテーブルに日付のsdでフィールドを追加する、場所はmtの後ろとする
addressテーブルのnameのデータ型を40文字に変更する
addressテーブルのtelのフィールド名をmailに変更する、データ型は50文字とする
musicテーブルのnumのフィールドを削除する
luckyテーブルを削除する
shopテーブルからcode・name・priceを表示する
shopテーブルから、priceが120のname・makerを表示する
musicテーブルにnameが「sample」、mtが3分44秒となるデータを追加する